// import { z } from 'zod';

// const tareaSchema = z.object({
//     // Hacer que el id sea único:
//     id: z.string().uuid(),

//     // Validar que el título de la tarea sea obligatorio y que la descripción tenga al menos 20 caracteres.
//     titulo: z.string().min(1),
//     descripcion: z.string().min(20),
// }).strict();

